package com.spelltype.keyboard.domain.model

enum class FrameStyle {
    NONE,
    // ═══ Box Styles ═══
    BOX,
    BOX_DOUBLE,
    BOX_ROUNDED,
    DOTTED,
    THICK,
    DASHED,
    CORNER,

    // ═══ Symbol Borders ═══
    STAR,
    DIAMOND,
    HEARTS,
    FLOWERS,
    MUSIC,
    SNOWFLAKE,
    CROSS,
    ARROW,
    BRACKET,

    // ═══ Emoji Borders ═══
    LEAF,
    SPARKS,
    CROWN,
    SWIRL,
    MOON,
    SUN,
    GALAXY,
    ANCHOR,
    SKULL,
    COFFEE,
    FISH,
    CLOUD,
    FIRE,
    PARTY,
    BALLOON,
    GHOST,
    FLORAL,
    GEM,
    CLOVER,
    LINE_BORDER,

    // ═══ NEW: 50+ Creative Keyboard Styles ═══

    // 💔 Heart Alphabet — letters become hearts
    HEART_ALPHABET,
    // 💕 Love Mode — everything wrapped in love
    LOVE_MODE,
    // 💔 Break Mode — broken heart style
    BREAK_MODE,
    // 🔥 Revenge Mode — aggressive bold style
    REVENGE_MODE,
    // 🌧️ 5D Rain Mode — rain effect text
    RAIN_MODE,
    // 🌸 Cherry Blossom — sakura themed
    CHERRY_BLOSSOM,
    // ❄️ Ice Mode — frozen text style
    ICE_MODE,
    // 🌈 Rainbow Mode — colorful text
    RAINBOW_MODE,
    // ⚡ Thunder Mode — electric style
    THUNDER_MODE,
    // 🌙 Night Mode — moonlight style
    NIGHT_MODE,
    // 🔮 Crystal Mode — crystal clear style
    CRYSTAL_MODE,
    // 🦋 Butterfly Mode — butterfly themed
    BUTTERFLY_MODE,
    // 🌺 Rose Mode — rose themed
    ROSE_MODE,
    // 💎 Diamond Mode — diamond encrusted
    DIAMOND_MODE,
    // 🎭 Shadow Mode — shadow text
    SHADOW_MODE,
    // 🔥 Flame Mode — fire text
    FLAME_MODE,
    // ⭐ Starry Mode — starry night
    STARRY_MODE,
    // 🌊 Wave Mode — ocean wave
    WAVE_MODE,
    // 🍂 Autumn Mode — fall leaves
    AUTUMN_MODE,
    // ❄️ Winter Mode — winter frost
    WINTER_MODE,
    // 🌸 Spring Mode — spring flowers
    SPRING_MODE,
    // ☀️ Summer Mode — summer vibes
    SUMMER_MODE,
    // 🎃 Halloween Mode — spooky
    HALLOWEEN_MODE,
    // 🎄 Christmas Mode — festive
    CHRISTMAS_MODE,
    // 💀 Gothic Mode — dark gothic
    GOTHIC_MODE,
    // 🏰 Medieval Mode — medieval style
    MEDIEVAL_MODE,
    // 🚀 Space Mode — cosmic style
    SPACE_MODE,
    // 🎮 Gamer Mode — gaming style
    GAMER_MODE,
    // 🎵 Music Mode — musical notes
    MUSIC_MODE,
    // 🌴 Tropical Mode — tropical vibes
    TROPICAL_MODE,
    // 🐾 Animal Mode — animal themed
    ANIMAL_MODE,
    // 🍕 Food Mode — food themed
    FOOD_MODE,
    // ⚽ Sports Mode — sports themed
    SPORTS_MODE,
    // 🎨 Art Mode — artistic style
    ART_MODE,
    // 💻 Tech Mode — tech/cyber style
    TECH_MODE,
    // 🌍 World Mode — world themed
    WORLD_MODE,
    // 🎯 Target Mode — target/bullseye
    TARGET_MODE,
    // 🔑 Key Mode — key/lock themed
    KEY_MODE,
    // 💰 Money Mode — money themed
    MONEY_MODE,
    // 🎁 Gift Mode — gift/box themed
    GIFT_MODE,
    // 🌈 Pride Mode — rainbow pride
    PRIDE_MODE,
    // 🕶️ Stealth Mode — stealth/camo
    STEALTH_MODE,
    // 🎪 Circus Mode — circus themed
    CIRCUS_MODE,
    // 🏔️ Mountain Mode — mountain/nature
    MOUNTAIN_MODE,
    // 🌅 Sunset Mode — sunset colors
    SUNSET_MODE,
    // 🌌 Aurora Mode — aurora borealis
    AURORA_MODE,
    // 🔥 Phoenix Mode — phoenix rebirth
    PHOENIX_MODE,
    // 🐉 Dragon Mode — dragon themed
    DRAGON_MODE,
    // 👻 Haunted Mode — haunted/ghost
    HAUNTED_MODE,
    // 🎭 Drama Mode — theatrical
    DRAMA_MODE
}
